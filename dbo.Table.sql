CREATE TABLE [dbo].[Table]
(
	[Id] INT NOT NULL PRIMARY KEY, 
    [UserName] VARCHAR(50) NOT NULL, 
    [Password] INT NOT NULL, 
    [Email] VARCHAR(50) NOT NULL, 
    [Confirm Password] INT NOT NULL
)
